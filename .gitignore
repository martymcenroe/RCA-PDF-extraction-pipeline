# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Claude Code temporary files
tmpclaude-*

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
ENV/

# Node.js
node_modules/
npm-debug.log
yarn-error.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
*.local

# Logs
*.log
logs/

# Coverage
.coverage
htmlcov/
coverage.xml

# Test artifacts
.pytest_cache/
.tox/

# Build artifacts
*.generated.json

# PDF extraction output
# Ignore stray files at root of output (use spec/ and extended/ subdirs)
data/output/*.db
data/output/*.json
data/output/*.csv
data/output/*_images/
# spec/ and extended/ are committed - see wiki for structure

# Large input files (none currently ignored)

# Playwright MCP
.playwright-mcp/

# Word documents (commit PDFs only)
*.docx

# Unleashed session transcripts (auto-generated, untracked)
data/unleashed/

# Session logs — internal-only; never publish (fleet sweep 2026-05-23, #59)
docs/session-logs/
docs/dom-dumps/

# Agent/automation tooling — not part of the published project
CLAUDE.md
GEMINI.md
.unleashed.json
.claude/
.agentos/

# ── Never publish ─────────────────────────────────────────────────────────
# Private research/planning material. Keep on local disk if present, but it
# must NEVER enter this public repo. These paths are ignored so they cannot be
# staged, committed, or pushed even if a working copy contains them.
docs/research/
docs/context/init/exercise-04-instructions.txt
*.docx
*Business Plan*
*Strategic Business*
*Forensic Integrity*
*Deep Research*
*deep-research*
